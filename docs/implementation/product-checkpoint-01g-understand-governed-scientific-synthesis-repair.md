# PRODUCT-CHECKPOINT-01G — UNDERSTAND governed scientific synthesis repair

Nature: `LEVEL_3_IMPLEMENTATION_EVIDENCE`

Normative authority: `NONE`

Scientific qualification: `NOT_PERFORMED`

## Decision

The bounded repair is technically ready for normal promotion to `main` and a manual product test. The deployed decision remains pending until the normal Git/Vercel publication is verified.

`FIRST_DIVERGENT_STAGE = UNDERSTAND_CONVERSATION_KNOWLEDGE_PROJECTION_COMPOSITION`

`SCIENTIFIC_PASS = NO`

`PD011_PASS = NO`

`WAVE_2_AUTHORIZED = NO`

`PRODUCT_TRACE_INTEGRATION = ABSENT`

## Authorities

Read in the required order:

1. `0. NOXIA — SOURCE-OF-TRUTH-INDEX.md`;
2. `NOXIA — Charte fondatrice`;
3. `NOXIA Protocol Designer — Scientific Product Manifesto V2`;
4. `Editorial Engine — Architecture Manifesto`;
5. PD-003 V2 and its Ownership Matrix;
6. PD-004;
7. PD-009;
8. RDE-001 v1.1;
9. RDE-002 v1.1;
10. KE-001 v1.0.

No admitted authority contradiction was found. The implementation reports consumed below remained Level 3 evidence only.

## Git baseline

- branch: `protocol-designer-canonical-ingestion`;
- HEAD at mission start: `312b4b9c45de57ed3a6339dcc703f79955fbc36c`;
- `origin/protocol-designer-canonical-ingestion`: same SHA;
- `origin/main`: same SHA;
- local `main`: `9be06edca1a7500ab7a43d065e94241e91d67bec`;
- tracked worktree: clean before the repair;
- 32 collapsed untracked paths, representing the known historical artifact set, preserved without clean, stash, move, deletion or mass-add.

The topology permits a normal fast-forward publication of the repair commit from the integration branch to `origin/main`; no rebase, force-push or history rewrite is required.

## 01F / 01F-R findings consumed

The repair consumes `product-checkpoint-01f-understand-scientific-synthesis-forensic.md`, including its 01F-R authority reconciliation:

- Knowledge owns scientific selection, qualification and governed structured synthesis;
- Document owns faithful UNDERSTAND composition and linguistic realization;
- React owns no scientific proposition;
- Case B already contained a directly relevant governed MR/CT assertion, sources, limitations, gaps and controversy;
- the product exposed a generic fallback and a mechanical item list instead of composing those governed elements.

## Root cause repaired

`synthesizeKnowledge` previously copied all applicable assertions and documentary statements into an undifferentiated conclusion list. `projectUnderstandResult` then ignored that list for partial coverage and emitted a generic fallback. `readableKnowledgeReply` appended the first atomic items mechanically.

The repair adds deterministic response roles and a faithful sentence-level projection. It does not add corpus content, scientific relations, external evidence, an LLM fallback or a new owner.

## Knowledge synthesis changes

- Knowledge runtime version: `1.2.0 → 1.2.1`.
- Each conclusion now preserves item kind, concept refs, sources, locator, limitations and an optional governed semantic relation.
- A `responseProfile` separates direct response, supporting context, contextual limits, contradictions and blocking gaps.
- Response state distinguishes direct, partial, contradictory, unavailable/unknown and no-applicable Knowledge.
- Cross-branch direct relations are recognized from typed semantic predicates and branch modalities, not from a Case B answer fixture.
- No assertion, source, locator, gap or contradiction is deleted or rewritten in the KnowledgeResult.

## Conversation projection changes

- `PRODUCT_UNDERSTAND_KNOWLEDGE_PRESENTATION` advances additively from `1.0.0` to `1.1.0`.
- `projectUnderstandResult` consumes `responseProfile` before any fallback.
- The complete normalized question is preserved in `requestSummary`.
- Atomic Knowledge items remain in the presentation payload for audit/replay, while only answer-linked items are selected for the visible evidence disclosure.
- Provider-not-applicable is presented as “Réponse non disponible dans ce contexte”, not as a global judgment that the user’s scientific context is itself inapplicable.

## Linguistic realization changes

Document renders known governed semantic identifiers and predicates into bounded French sentences. It neither creates a new assertion nor increases certainty. Unknown predicates remain in their governed source wording instead of being guessed.

The assistant reply now contains, in order:

1. the composed governed answer;
2. the preserved user question;
3. the bounded internal-corpus conclusion.

It no longer dumps the first four atomic items and first three gaps into the primary conversation reply.

## Sentence-to-evidence preservation

Every answer statement carries a structured support envelope containing the applicable combination of:

- Knowledge item refs;
- source refs;
- localizers;
- gap refs;
- contradiction refs;
- limitation refs;
- coverage refs.

Direct and supporting scientific statements are required by test to retain item, source and locator refs. Gap and coverage statements remain explicitly linked to their typed gap/coverage records.

## Case B

Exact fixture:

> Je voudrais comprendre dans quelles situations l’ECV mesuré en IRM cardiaque et l’ECV mesuré en CT cardiaque sont réellement comparables pour étudier une fibrose myocardique diffuse

Observed deterministic behavior:

- ECV-IRM and ECV-CT both remain explicit;
- the governed assertion that their measurement methods are distinct becomes the direct answer element;
- MR method dependence and required T1 inputs are exposed as documented dependencies;
- `DIRECT_COMPARISON` remains an open gap;
- no universal interchangeability or common threshold is claimed;
- the encoded controversy remains separate and unresolved;
- diffuse myocardial fibrosis remains in the preserved question;
- Project writes, protocol projections and external calls remain zero.

This is a technical fidelity finding, not a scientific PASS.

## Case A

The runtime recognizes IRM, no-reflow, microvascular obstruction and stenting, but returns zero applicable assertions and zero applicable documentary statements for the exact requested relation/context. The projection therefore reports `NO_APPLICABLE_KNOWLEDGE` and preserves the requested objects without inventing a no-reflow/MVO distinction.

`PARTIAL_ANSWER` is not claimed because the current typed result contains no applicable answer branch. Nearby sources remain auditable but are not promoted into the answer.

## No-applicable-knowledge case

An unmapped local question produces one bounded gap statement, typed `NO_APPLICABLE_KNOWLEDGE`, with gap/coverage refs and no external fallback. Domain-gate, privacy and biomarker-selection boundaries retain their dedicated existing messages.

## Project/QRY non-regression

Targeted regression proves:

- UNDERSTAND uses local Knowledge;
- Project write count = 0;
- protocol projection count = 0;
- external/provider call count = 0;
- existing canonical Project remains byte-identical;
- pending QRY remains byte-identical and is not captured;
- explicit no-study/no-protocol remains governing;
- explicit transition to DESIGN_STUDY remains possible with preserved scientific context;
- out-of-domain and privacy boundaries remain fail-closed.

## Atomic evidence preservation

Case B retains 46 atomic/documentary presentation items. The primary visible evidence disclosure is a bounded subset linked to the composed answer; the full item registry, identities, sources, evidence relations, locators, applicability, contradictions, gaps and provenance remain in the product presentation object.

## Retrieval

`RETRIEVAL_WIDTH = UNCHANGED`

No threshold, provider selection, ranking rule, corpus or query-plan relaxation was modified.

## UX explicitly not redesigned

`UNDERSTAND_PROGRESSIVE_DISCLOSURE_UX = TO_REDESIGN`

Only the active response composition and its answer-linked evidence subset changed. The general panels, information architecture and broader progressive-disclosure design remain outside 01G.

## Legacy microcopy disposition

`OUT_OF_SCOPE_SECONDARY_FINDING`

No unrelated legacy microcopy surface was modified.

## Tests

Final targeted run:

- 10 selected test files;
- 9 passed, 1 historical file skipped;
- 81 tests passed;
- 7 tests skipped;
- 0 failed.

The new 01G suite contains 6 deterministic tests covering Case B synthesis, sentence-to-evidence, atomic preservation, Case A, no-applicable Knowledge and synthesis determinism.

An intermediate run exposed two regressions in the existing NumPy Domain Gate and patient-level privacy messages. The generic no-applicable fallback was narrowed before finalization; both mandatory cases pass in the final run.

Targeted ESLint: PASS.

`git diff --check`: PASS.

Targeted secret scan: 0 finding.

## Typecheck/build

- default local TypeScript check for the changed application graph: PASS;
- repository `npm run typecheck`: reproduces exactly 2 historical `TS2698` errors in `w1-qual-02h1r-deterministic-checker.test.ts` lines 37 and 50;
- no 01G TypeScript error observed;
- production build: PASS, 1,989 modules transformed;
- existing build warnings retained: stale Browserslist data, Rollup pure-annotation notices, one CSS syntax warning and large-chunk notices.

The historical `TS2698` debt was not modified.

## Cost

`EXTERNAL_LLM_API_CALLS = 0`

`OPENAI_API_CALLS = 0`

`CHATGPT_API_CALLS = 0`

`GEMINI_API_CALLS = 0`

`OTHER_LLM_PROVIDER_CALLS = 0`

`SCIENTIFIC_OR_PROVIDER_NETWORK_CALLS = 0`

`NEW_SCIENTIFIC_CAMPAIGNS = 0`

`NEW_BENCHMARKS = 0`

`BROAD_REPLAY = 0`

The mission’s `NETWORK_CALLS = 0` cost constraint is preserved for scientific, provider and research activity. The separately required Git publication and Vercel Production verification necessarily use operational network access and must be reported as deployment operations, not hidden as zero calls.

## Remaining limitations

- No scientific quality or universal comparability claim was evaluated.
- Case A remains scientifically unanswered by current applicable local Knowledge.
- Case B still lacks a general direct MRI/CT comparison and preserves an unresolved controversy.
- The response lexicon covers only governed identifiers/predicates already encountered; unknown relations remain source-form text.
- General UNDERSTAND progressive-disclosure UX remains open.
- Product TRACE integration remains absent.
- The two historical `TS2698` typecheck debts remain.

## Git

Expected atomic commit message:

`fix(protocol-designer): compose governed understand synthesis`

This report belongs to the same atomic repair commit. No force-push, rebase, merge commit or history rewrite is authorized or required.

## Production deployment

Target: normal Vercel Production publication from `origin/main`, after the atomic commit is pushed by normal fast-forward. Production smoke scope is limited to deployed SHA verification, build status, `/protocol-designer/demo` load, blocking runtime errors and secret exposure. The scientific user cases are reserved for manual testing and must not be replayed automatically after deployment.
