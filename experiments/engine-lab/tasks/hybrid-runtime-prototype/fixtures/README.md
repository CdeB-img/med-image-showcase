# Visible corpus binding

This prototype reads the already exposed I01–I08 conversations directly from
`experiments/semantic-engine-comparison/results/common-contract-ablation-02/scenario-pack-frozen.json`.

It does not copy, paraphrase or modify the scenarios. The experiment manifest
records the source digest before provider execution. No blind benchmark path is
part of this binding.
