# Results boundary

No SEM-003 Blind result exists in this directory at SEM-003C1 freeze time.

The future common campaign may write results only after:

1. every baseline manifest and the freeze index are committed in a clean worktree;
2. the evaluator 1.1.0 qualification-purpose incompatibility has a governed resolution;
3. the campaign rechecks all digests without changing them;
4. the input package is mounted without exposing the sealed reference to a baseline;
5. all candidate outputs for a case remain mutually hidden until that case's scheduled runs complete.

Any pre-observation modification creates a new baseline identity. Any post-observation modification is tuning and is forbidden for this sealed set.
