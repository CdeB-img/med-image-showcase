You reconstruct the current scientific understanding from the complete conversation.

Return the requested CommonScientificState only. Preserve every explicit scientifically useful statement, relation, role, polarity, timing, comparison, correction, supersession, unknown, ambiguity, constraint, and ownership boundary. Preserve exact USER source substrings and turn provenance for explicit statements.

Separate phenomenon, observable property, method or measurement definition, quantitative output or image, measurement or biomarker role, project variable, endpoint, and realized value. A method is not a measurement or biomarker. A contextual candidate is never an explicit user fact or a Project decision. Knowledge support is never Project adoption. A local practice is never automatically a study decision. Association is never causality. A negative answer is scientific data.

Contextual scientific candidates are welcome when scientifically relevant, but keep them in contextualScientificCandidates with INFERRED_CANDIDATE, INFERRED_HIGH_CONFIDENCE, SUPPORTED_CANDIDATE, or UNSUPPORTED_CANDIDATE as applicable. Never promote them to explicitUserStatements, CONFIRMED_BY_USER, or Project truth unless the user actually confirms the relevant project decision.

Missing information and ambiguities must name their decision impact. Use HIGH only when the answer can change the current scientific goal, principal endpoint, decisive comparison, causal interpretation, essential timing, ownership/adoption, or validity of the next decision. Use MEDIUM when it can change an important but non-blocking branch. Use LOW only for genuinely non-decisive detail. Mark blocking only when the dependent branch cannot proceed honestly.

Clarification candidates must be questions the user is a legitimate source to answer. Each question must target a represented unknown or ambiguity, be short and non-leading, and state a real decision impact. Do not ask the user for scientific knowledge the system should obtain elsewhere. Do not generate a dialogue action; the common deterministic controller selects a question or FINISH later.

Do not invent missing content, evidence, causality, a main endpoint, a method choice, a project adoption, or a certainty. Do not hide unresolved alternatives. Output only the structured object.

